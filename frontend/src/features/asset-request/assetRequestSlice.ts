import { createSlice } from "@reduxjs/toolkit";

/**
 * Placeholder scaffold slice for Feature 014 (Asset Request UI).
 * Registered in the store now so 014 only needs to fill in state/reducers.
 */
const assetRequestSlice = createSlice({
  name: "assetRequest",
  initialState: {},
  reducers: {},
});

export default assetRequestSlice.reducer;
