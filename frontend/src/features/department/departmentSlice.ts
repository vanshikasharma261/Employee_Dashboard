import { createSlice } from "@reduxjs/toolkit";

/**
 * Placeholder scaffold slice for Feature 012 (Department Management UI).
 * Registered in the store now so 012 only needs to fill in state/reducers.
 */
const departmentSlice = createSlice({
  name: "department",
  initialState: {},
  reducers: {},
});

export default departmentSlice.reducer;
