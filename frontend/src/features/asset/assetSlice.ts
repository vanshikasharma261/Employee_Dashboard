import { createSlice } from "@reduxjs/toolkit";

/**
 * Placeholder scaffold slice for Feature 013 (Asset Management UI).
 * Registered in the store now so 013 only needs to fill in state/reducers.
 */
const assetSlice = createSlice({
  name: "asset",
  initialState: {},
  reducers: {},
});

export default assetSlice.reducer;
