import { createSlice } from "@reduxjs/toolkit";

/**
 * Placeholder scaffold slice for Feature 011 (Employee Management UI).
 * Registered in the store now so 011 only needs to fill in state/reducers.
 */
const employeeSlice = createSlice({
  name: "employee",
  initialState: {},
  reducers: {},
});

export default employeeSlice.reducer;
